# The study site now lives at thedegreegap.com/study/ so it sits inside the
# main domain rather than on a separate subdomain.
#
# This Netlify site is kept alive purely to redirect the old subdomain. Every
# path maps one-to-one onto its new address, so each old URL redirects straight
# to its equivalent page rather than dumping everyone on the homepage.
#
# The path-level redirects that used to live here (personal-statements, removed
# location pages) now run on Apache — see static/.htaccess, which Hugo ships
# into the deployed site. Old subdomain URLs therefore take two hops at most:
# subdomain -> /study/<same path> -> final destination.

/*  https://thedegreegap.com/study/:splat  301!
